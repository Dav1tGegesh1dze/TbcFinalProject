<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="ShapeAppearanceOverlay.MaterialComponents.CircleImageView" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">20%</item>
    </style>
    <style name="CustomSpinnerStyle" parent="Widget.AppCompat.Spinner">
        <!-- Set your custom background -->
        <item name="android:background">@drawable/spinner_background</item>
        <!-- Optional: adjust padding if needed -->
        <item name="android:padding">10dp</item>
        <!-- Optional: set popup background for the dropdown list -->
        <item name="android:popupBackground">@drawable/spinner_background</item>
    </style>
</resources>